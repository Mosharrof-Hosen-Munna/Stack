// For test authencation error. I have used it like as a playground.



// import { useState } from "react";
// import { registrationValidation } from "../validations/authValidator";

// const useAuth = () => {
//   const [regError, setRegError] = useState({});
//   const registration = (name, email, password) => {
//     const validateData = registrationValidation(name, password);
//     if (validateData.name || validateData.password) {
//       setRegError(validateData);
//       return;
//     }
//   };

//   return {
//     registration,
//     regError
//   }
// };

// export default useAuth
